모든 클래스가 Object 클래스를 상속 받는 이유는 다음과 같다.

공!통! 기능 제공
    객체의 정보를 제공하는 toString()
    객체의 같음을 비교하는 equals()
    객체의 클래스 정보를 제공하는 getClass()
    기타 여러가지 기능
다형성의 기본 구현
    타입이 다른 객체들을 어디간에 보관해야 한다면 바로 Object에 보관하기

메서드 오버라이딩이 안되기 때문에 다형성을 활용하기에는 한계
언제 활용 하는게 좋을까?

Object 타입을 사용한 덕분에 세상의 모든 객체를 담을 수 있는 배열을 만들 수 있었다.
